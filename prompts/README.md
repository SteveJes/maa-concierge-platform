# Prompt Library

Keep prompt templates versioned and reviewable.

- `system.base.md` defines global fallback behavior.
- `packages/prompts` contains importable prompt constants/helpers.
- Client overrides belong in `clients/<tenant>/`.
