node_modules
.pnpm-store
.env
.env.*
!.env.example
coverage
dist
*.log
.DS_Store

.claude/*
# Track the shared subagent definitions — Steve + Daphné rely on them.
!.claude/agents/
!.claude/agents/**

# Next.js / Turbo / build artifacts
.next/
.turbo/
*.tsbuildinfo

# Playwright artifacts
test-results/
playwright-report/
e2e/screenshot-*.png
e2e/report-*.json

# Local regression output
apps/api/regression-report.json

# Local VAPI prompt exports (regenerated by `pnpm.cmd --filter @platform/api vapi:prompts`)
_vapi-prompts/

# Sentinel — local run reports + auto-generated scenarios (regenerated on demand)
apps/api/_sentinel-runs/
apps/api/src/scenarios/generated/

# Inbox — raw source documents from tenants (PDFs, emails, notes) before extraction
apps/api/_inbox/

# Local PDF-expiry cron output (Daphné batch 2026-05-27 §9B.7)
apps/api/_alerts/

# Cost tracking — daily OpenAI usage JSONL files (rotated by day)
apps/api/_costs/
