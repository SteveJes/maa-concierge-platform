import type { ApprovedSourceConfig } from "@platform/schemas";

export async function ingestPdfFiles(_source: ApprovedSourceConfig): Promise<string[]> {
  // Placeholder: collect raw PDF text for later normalization.
  return [];
}
