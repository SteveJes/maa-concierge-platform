import type { ApprovedSourceConfig } from "@platform/schemas";

export async function ingestWebsitePages(_source: ApprovedSourceConfig): Promise<string[]> {
  // Placeholder: collect raw page content for later normalization.
  return [];
}
