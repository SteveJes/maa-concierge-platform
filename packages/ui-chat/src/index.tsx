import type { JSX } from "react";

export function ChatShell(): JSX.Element {
  return (
    <section style={{ border: "1px solid #d1d5db", borderRadius: 12, padding: 16, background: "white" }}>
      <p>Reusable chat UI package placeholder.</p>
    </section>
  );
}
