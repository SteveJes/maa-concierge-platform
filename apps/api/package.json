{
  "name": "@platform/api",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "node -e \"console.log('lint placeholder: api')\"",
    "test": "node -e \"console.log('test placeholder: api')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ingest:web": "tsx src/scripts/ingestion-persistence-smoke.ts maa",
    "ingest:web:smoke": "tsx src/scripts/ingestion-retrieval-smoke.ts maa"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "@fastify/cors": "^11.0.0",
    "@platform/schemas": "workspace:*",
    "@platform/agent-runtime": "workspace:*",
    "@platform/config": "workspace:*",
    "@platform/retrieval": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
